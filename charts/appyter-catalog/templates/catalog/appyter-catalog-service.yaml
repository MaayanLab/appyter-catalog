apiVersion: v1
kind: Service
metadata:
  name: appyter-catalog
  namespace: {{ .Values.appytercatalog.namespace }}
spec:
  selector:
    app: appyter-catalog
  type: ClusterIP
  ports:
  - name: appyter-catalog
    port: 80
    protocol: TCP
    targetPort: 80